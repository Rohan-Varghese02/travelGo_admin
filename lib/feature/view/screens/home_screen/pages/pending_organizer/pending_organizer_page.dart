import 'package:flutter/material.dart';

class PendingOrganizerPage extends StatelessWidget {
  const PendingOrganizerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Pending Organizer'),),
    );
  }
}